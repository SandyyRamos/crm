<div class="container mt-5">
  <h1 class="text-center mb-4">Nuevo Prospecto</h1>

  <%= simple_form_for @lead do |form| %>
    <div class="form-group">
      <%= form.input :name, label: "Nombre", input_html: { class: "form-control" } %>
    </div>

    <div class="form-group">
      <%= form.input :email, label: 'Email', input_html: { class: "form-control" } %>
    </div>

    <div class="form-group">
      <%= form.input :phone, label: "Teléfono", input_html: { class: "form-control" } %>
    </div>

    <div class="form-group mt-3">
      <%= form.button :submit, @lead.new_record? ? "Crear" : "Editar", class: 'btn btn-success' %>
    </div>
  <% end %>

  <div class="text-center mt-3">
  </div>
</div>
