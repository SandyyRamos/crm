<h1>Conversations#index</h1>
<p>Find me in app/views/conversations/index.html.erb</p>
<h3>Conversaciones</h3>
<%= form_with model: [@lead, @conversation], local: true do |form| %>
  <div class="form-group">
    <%= form.text_area :detail, rows: 5, class: "form-control" %>
  </div>
  <%= form.submit "Subir información", class: "btn btn-primary" %>
<% end %>
<h3>Conversaciones</h3>
<%= form_with model: [@lead, @conversation], local: true do |form| %>
  <div class="form-group">
    <%= form.text_area :detail, rows: 5, class: "form-control" %>
  </div>
  <%= form.submit "Subir información", class: "btn btn-primary" %>
<% end %>

<ul>
  <% @conversations.each do |conversation|%>
  <li>
    <%= conversation.detail %>
<% end %>

</ul>
